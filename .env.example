# Generate URL-safe values with: openssl rand -hex 32
POSTGRES_PASSWORD=
SHARED_SECRET=
# Optional; no admin or demo account is created unless explicitly configured.
ADMIN_SEED_PASSWORD=
# Optional when port 5173 is occupied:
# FRONTEND_PORT=15173
